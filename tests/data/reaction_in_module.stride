_domainDefinition RootDomain {
	framework: _JitFramework
	# requiresResources: [_RootContext]
	# providesResources: [_DomainFunction]
	rate: -1 # -1 is 'one-shot/final' domain
    substitutions: [ ]
    domainIncludes: []
    inputs: [
        signal In {domain: RootDomain}
    ]
    outputs: [
        signal Out {domain: RootDomain}
    ]
	domainFunctionCode: ''
}

module Max {
    ports: [
        mainInputPort InputPort {
            block: Input
        }
        mainOutputPort OutputPort {
            block: Output
        }
    ]
    blocks: [
        signal Input {}
        signal Output { default: 0 reset:Reset }
    ]
    streams: [
        [Input, 5.0] >> Greater() >> reaction { Input >> Output; };

    ]
}

In >> Max() >> Out;
